#include <stdio.h>

/**
 * main - prints out a story of hare and tortoise
 *
 * Return: Always 0.
 */
int main (void)
{

	printf("(A tortoise, having a pretty good sense of a hare's nature, challenges one to a race.)\n");
	return (0);
}
