#include "main.h"

/**
 * main - prints numbers
 *
 * Return: Always 0
 */
void print_most_numbers(void)
{

	int y;

	for (y = 48; y < 57; y++)
	{
	_putchar(y);
	
	if (y == 50 || y == 52) 
		continue; 
	}

	_putchar('\n');
}
