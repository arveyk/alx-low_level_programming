#include "main.h"

/**
 * _isdigit - checks for digit
 *
 * Return: 1 if true, 0 if false
 * @c: varible to be evaluated
 */

int _isdigit(int c)
{

	if (c >= 48 && c <= 57)
		return (1);
	else
		return (0)
}
