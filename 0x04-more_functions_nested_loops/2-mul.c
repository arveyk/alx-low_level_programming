#include <stdio.h>

/**
 * mul - find the product of two numbers
 *
 * Return: product.
 * @a: first variable
 * @b: second variable
 */
int mul(int a, int b)
{

	int prod;

	prod = (a * b);

	return  (prod);
}
