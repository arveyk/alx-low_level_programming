Structures are compound data structure. They are composed of multiple data types under one name.
The keyword struct is used to create new structures.
It defines a new data types
eg.
typdef struct pin point;
point{
int p;
char i;
char *c;
};
