#include "main.h"

/**
 * mul - evaluates the product of two numbers
 * @a: first number
 * @b: second number
 *
 * Return: product of the two numbers
 */
int mul(int a,  int b)
{

	return (a * b);
}
