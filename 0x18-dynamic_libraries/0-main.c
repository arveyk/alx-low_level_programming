#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints length of string  by dynamic call to _strlen
 *
 * Return: Always EXIT_SUCCESS.
 */
int main(void)
{

	printf("%d\n", _strlen("My Dyn Lib"));
	return (EXIT_SUCCESS);
}
