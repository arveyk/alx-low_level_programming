#include "main.h"
#include <stdio.h>

/**
 * reverse_array - reverses the elements of an array
 *
 * Return: no value
 * @a: pointer to array
 * @a: int to help in iteration
 */
void print_number(int n)
{
	
	_putchar((char)n);
}
