#include "main.h"
#include <string.h>

/**
 * rev_string - reverses string
 *
 * Return: none
 * @ s: pointer to string to be reversed
 */
void rev_string(char *s)
{

	int len = strlen(s);
	char z = *s;
	
	while ()
		*s = z;

}
