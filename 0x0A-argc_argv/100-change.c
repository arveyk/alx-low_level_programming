#include <stdio.h>

/**
 * main- prints the minimum number of coin to make change
 *
 * Return: Always 0.
 */
int main(int argc, char **argv)
{
	int f = 0;
	int r = atoi(argv[1]);

	int z = r % 25;
	int x = r % 10;
	int c = r % 5;
	int v = r % 2;
	int b = r % 1;
		
	if (r > 25)
		change += (r / 25);
		{
			if (z > 10)
				change += (z / 10);
			if () 
	return (0);
}
